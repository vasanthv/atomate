const addSource = (sourceURL) => {};

const isRSSURL = (sourceURL) => {};
const findRSSURL = (sourceURL) => {};

const ifSourceExist = (sourceURL) => {};
const createSource = (source) => {};
const fetchRSSFeed = (rssURL) => {};

const saveItem = (item) => {};

const fetchItems = (sources) => {};
const fetchSources = (sources) => {};
